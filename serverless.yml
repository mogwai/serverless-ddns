service:
  name: dynamic-dns

plugins:
  - serverless-offline

package:
  individually: true
  excludeDevDependencies: true

provider:
  name: aws
  runtime: nodejs8.10
  region: eu-west-2
  memorySize: 128

functions:
  resetPasswordChange:
    handler: index.handler
    events:
      - http:
          path: /
          method: post
          cors: true